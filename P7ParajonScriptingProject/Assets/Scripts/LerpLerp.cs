using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LerpLerp : MonoBehaviour
{
    /*
     
    This right here will give 4

    float result = Mathf.Lerp(3f, 5f, 0.5f);

    Makes an array of numbers

    Vector3 from = new Vector3 (1f, 2f, 3f);
    
    Makes another array of numbers

    Vector3 to = new Vector3 (5f, 6f, 7f);

    
    
    Here result = (4, 5, 6)

    It'll do the lerp thing for 1-4, 2-5, and 3-6
    Vector3 result = Vector3.Lerp (from, to, 0.75f);

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        It'll give a number based on the intesity of the light if it's 4 then you would get 6
        light.intensity = Mathf.Lerp(light.intensity, 8f, 0.5f);

        It'll give a number based on the intensity of light and the percentage would be based on the the seconds passed so it would get slower over time
        light.intensity = Mathf.Lerp(light.intensity, 8f, 0.5f * Time.deltaTime);
    } */
}   
